name: Günlük Artışlı Veri Güncellemesi

on:
  schedule:
    # Her gün saat 16:00 UTC (Türkiye saati 19:00 - UTC+3)
    - cron: '0 16 * * 1-5'
  workflow_dispatch:
    inputs:
      force_run:
        description: 'Zorla çalıştır (manuel tetikleme)'
        required: false
        default: 'false'

jobs:
  update-daily-data:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: [3.11]

    steps:
    - name: Repository'yi klonla
      uses: actions/checkout@v4

    - name: Python ${{ matrix.python-version }} kurulumu
      uses: actions/setup-python@v4
      with:
        python-version: ${{ matrix.python-version }}

    - name: Cache pip dependencies
      uses: actions/cache@v3
      with:
        path: ~/.cache/pip
        key: ${{ runner.os }}-pip-${{ hashFiles('requirements.txt') }}
        restore-keys: |
          ${{ runner.os }}-pip-

    - name: Bağımlılıkları yükle
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt

    - name: TradingView ile artışlı veri güncellemesi (100 bar)
      env:
        SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
        SUPABASE_KEY: ${{ secrets.SUPABASE_KEY }} 
        TV_USERNAME: ${{ secrets.TV_USERNAME }}
        TV_PASSWORD: ${{ secrets.TV_PASSWORD }}
        INCREMENTAL_FETCH_BARS: 5
        FORCE_RUN: ${{ github.event.inputs.force_run }}
      run: |
        echo "Artışlı veri güncellemesi başlatılıyor (INCREMENTAL_FETCH_BARS=5)..."
        python tv_data_supabase.py
      continue-on-error: false

    - name: Güncelleme sonuçlarını raporla
      if: always()
      run: |
        echo "=== GÜNCELLENMIŞ VERİ RAPORU ==="
        if [ -f "update_results.json" ]; then
          echo "Sonuç dosyası başarıyla oluşturuldu."
          echo ""
          echo "📊 GÜNCEL VERİ ÖZETİ:"
          cat update_results.json
          echo ""
          echo "📈 İŞLEM İSTATİSTİKLERİ:"
          echo "├─ Toplam işlenen sembol sayısı: $(cat update_results.json | jq -r '.total_symbols_processed // "0"' | head -1)"
          echo "├─ Başarılı güncellemeler: $(cat update_results.json | jq -r '.successful_updates // "0"' | head -1)"
          echo "└─ Hatalı işlemler: $(cat update_results.json | jq -r '.failed_updates // "0"' | head -1)"
          
          # Error durumunda uyarı
          FAILED=$(cat update_results.json | jq -r '.failed_updates // 0' | head -1)
          if [ "$FAILED" -gt 0 ]; then
            echo ""
            echo "⚠️  UYARI: $FAILED hatalı işlem tespit edildi!"
            echo "Hata detayları için log dosyalarını kontrol edin."
          fi
        else
          echo "❌ HATA: Güncelleme sonuç dosyası bulunamadı!"
          echo "İşlem log dosyalarını kontrol edin."
          ls -la *.log 2>/dev/null || echo "Log dosyası bulunamadı."
        fi

    - name: İşlem durumunu logla
      if: always()
      run: |
        echo "=== İŞLEM ÖZET RAPORU ==="
        echo "⏰ İşlem zamanı (UTC): $(date -u)"
        echo "🕐 Türkiye saati: $(TZ='Europe/Istanbul' date)"
        echo "🔄 GitHub Run ID: ${{ github.run_id }}"
        echo "✅ İşlem durumu: ${{ job.status }}"
        echo "📍 Runner: ${{ runner.os }} - ${{ runner.arch }}"
        echo "🏷️  Workflow: ${{ github.workflow }}"
        echo "🌍 Commit: ${{ github.sha }}"
        echo ""
        echo "🎯 Sonuç: $( [ "${{ job.status }}" = "success" ] && echo "✅ Başarılı" || echo "❌ Başarısız" )"
