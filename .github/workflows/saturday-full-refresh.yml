name: Cumartesi Tam Yenileme

on:
  schedule:
    # Cumartesi günü saat 16:00 UTC (Türkiye saati 19:00 - UTC+3)
    - cron: '0 16 * * 6'
  workflow_dispatch:
    inputs:
      force_run:
        description: 'Zorla çalıştır (manuel tetikleme)'
        required: false
        default: 'false'

jobs:
  full-refresh-data:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: [3.11]

    steps:
    - name: Repository'yi klonla
      uses: actions/checkout@v4

    - name: Python ${{ matrix.python-version }} kurulumu
      uses: actions/setup-python@v4
      with:
        python-version: ${{ matrix.python-version }}

    - name: Cache pip dependencies
      uses: actions/cache@v3
      with:
        path: ~/.cache/pip
        key: ${{ runner.os }}-pip-${{ hashFiles('requirements.txt') }}
        restore-keys: |
          ${{ runner.os }}-pip-

    - name: Bağımlılıkları yükle
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt

    - name: TradingView tam yenileme (5000 bar)
      env:
        SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
        SUPABASE_KEY: ${{ secrets.SUPABASE_KEY }}
        TV_USERNAME: ${{ secrets.TV_USERNAME }}
        TV_PASSWORD: ${{ secrets.TV_PASSWORD }}
        FULL_REFRESH_N_BARS: 5000
      run: |
        echo "Tam yenileme başlatılıyor (FULL_REFRESH_N_BARS=5000)..."
        echo "Bu işlem hisse bölünmeleri ve fiyat düzeltmeleri için kritik."
        python tv_data_supabase.py --full-refresh
      continue-on-error: false

    - name: Tam yenileme sonuçlarını raporla
      if: always()
      run: |
        echo "=== TAM YENİLEME RAPORU ==="
        if [ -f "update_results.json" ]; then
          echo "✅ Sonuç dosyası başarıyla oluşturuldu."
          echo ""
          echo "📊 TAM YENİLEME VERİ ÖZETİ:"
          cat update_results.json
          echo ""
          echo "📈 DETAYLI İSTATİSTİKLER:"
          echo "├─ Tam yenilenmiş sembol sayısı: $(cat update_results.json | jq -r '.total_symbols_processed // "0"' | head -1)"
          echo "├─ Başarılı yenilemeler: $(cat update_results.json | jq -r '.successful_updates // "0"' | head -1)"
          echo "├─ Hatalı işlemler: $(cat update_results.json | jq -r '.failed_updates // "0"' | head -1)"
          echo "└─ Toplam güncellenen kayıt sayısı: $(cat update_results.json | jq -r '.total_records_updated // "0"' | head -1)"
          
          # Kritik hata kontrolü
          FAILED=$(cat update_results.json | jq -r '.failed_updates // 0' | head -1)
          if [ "$FAILED" -gt 0 ]; then
            echo ""
            echo "⚠️  KRİTİK UYARI: $FAILED hatalı işlem tespit edildi!"
            echo "Bu hisse senedi bölünmeleri ve fiyat düzeltmelerini etkileyebilir."
            echo "Hata detayları için log dosyalarını kontrol edin."
          fi
        else
          echo "❌ HATA: Tam yenileme sonuç dosyası bulunamadı!"
          echo "İşlem log dosyalarını kontrol edin."
          ls -la *.log 2>/dev/null || echo "Log dosyası bulunamadı."
        fi

    - name: Veri bütünlüğü kontrolü
      if: success()
      run: |
        echo "=== VERİ BÜTÜNLÜK KONTROLÜ ==="
        echo "🔍 Supabase bağlantısı ve veri bütünlüğü test ediliyor..."
        echo ""
        python test_supabase_connection.py
        echo ""
        echo "✅ Veri bütünlüğü kontrolü tamamlandı."

    - name: Tam yenileme özetini logla
      if: always()
      run: |
        echo "=== TAM YENİLEME ÖZET RAPORU ==="
        echo "⏰ İşlem zamanı (UTC): $(date -u)"
        echo "🕐 Türkiye saati: $(TZ='Europe/Istanbul' date)"
        echo "🔄 GitHub Run ID: ${{ github.run_id }}"
        echo "✅ İşlem durumu: ${{ job.status }}"
        echo "📍 Runner: ${{ runner.os }} - ${{ runner.arch }}"
        echo "🏷️  Workflow: ${{ github.workflow }}"
        echo "🌍 Commit: ${{ github.sha }}"
        echo ""
        echo "🎯 İşlem Özellikleri:"
        echo "├─ İşlem türü: Tam yenileme (FULL_REFRESH_N_BARS=5000)"
        echo "├─ Amaç: Hisse senedi bölünmeleri ve fiyat düzeltmeleri"
        echo "└─ Kritik işlem: Market veri doğruluğu için gerekli"
        echo ""
        echo "📊 Sonuç: $( [ "${{ job.status }}" = "success" ] && echo "✅ Başarılı - Veri bütünlüğü korundu" || echo "❌ Başarısız - Kontrol gerekli" )"
